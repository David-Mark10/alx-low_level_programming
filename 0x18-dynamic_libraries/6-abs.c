#include "main.h"

/**
 * _abs - func that computes the absolute value of an integer.
 * @n: The argument integer to be computed.
 *
 * Return: return absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
