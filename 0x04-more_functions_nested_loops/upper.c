#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 *
 * Return: Always 0 (Success)
 */
int _isdigit(int c)
{
	int i = 0;
	int a = 0;

	for (; i <= 9; i++)
	{
		if (c == i)
		{
			a = 1;
			printf("a");
		}
	return (0);
	}
}
